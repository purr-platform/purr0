--- Module schemas
--
-- Define the expected database schemas. These are automatically mapped
-- on the underlying data-base.


schema ^author {
  username: ^string.
  password: ^string.
  admin:    ^bool.
}

schema ^permalink->post {
  id:   ^string.
  post: ^post.
}

schema ^post {
  id:        ^string.
  permalink: ^string.
  title:     ^string.
  body:      ^string.
  author:    ^author.
  date:      ^date-time.
}
